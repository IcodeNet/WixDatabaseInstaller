the Wix_Components.bat will 
generate two files 

Components_Dir.wxs
Components_Project.wxs

that can be used to define (pasted) the Components in the inlcudes\Components.wxs

!!!!!
The Apply.sql  files are only used to manualy run the scripts against and SQL instance and SHOULD NOT be in the Components.wxs 
!!!!!!